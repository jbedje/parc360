server {
    listen 80;
    server_name vmi2804403.contaboserver.net;  # Remplacer par votre domaine

    # Logs
    access_log /var/log/nginx/parc360-access.log;
    error_log /var/log/nginx/parc360-error.log;

    # Frontend - React App
    location / {
        root /var/www/parc360/frontend/build;
        index index.html;
        try_files $uri $uri/ /index.html;

        # Cache control
        add_header Cache-Control "public, max-age=3600";
    }

    # Backend API
    location /api {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Static files caching
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        root /var/www/parc360/frontend/build;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json;
}

# Optional: SSL configuration (after setting up Let's Encrypt)
# server {
#     listen 443 ssl http2;
#     server_name vmi2804403.contaboserver.net;
#
#     ssl_certificate /etc/letsencrypt/live/vmi2804403.contaboserver.net/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/vmi2804403.contaboserver.net/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     # Same location blocks as above
# }
